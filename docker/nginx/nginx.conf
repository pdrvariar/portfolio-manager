server {
    listen 80;
    server_name localhost;
    
    # Sênior: O Nginx precisa apontar para onde o index.php reside fisicamente
    root /var/www/html/public; 
    index index.php index.html;

    error_log  /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        try_files $uri =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        
        # Conecta ao serviço 'app' definido no seu docker-compose.yml
        fastcgi_pass app:9000; 
        fastcgi_index index.php;
        include fastcgi_params;
        
        # Define o caminho real do arquivo para o PHP-FPM processar
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
        
        # Otimizações para respostas financeiras longas
        fastcgi_buffer_size 128k;
        fastcgi_buffers 4 256k;
    }

    location ~ /\. {
        deny all;
    }
}